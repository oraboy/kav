---
description: "Kav's interactive tutorial and health check. Checks the install (API keys, Chrome, Python dependencies), explains the six-step process, lists the commands, and offers to start a first story or open an existing one. Use when the author types /kav-start, says \"get started\", \"how does Kav work\", \"what can you do\", or has just finished installing."
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-start.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-start`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-start.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
