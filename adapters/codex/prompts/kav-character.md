---
description: "Bring a character into a story — take whatever exists (a portrait, an inspiration photo, or only a description) and build their reference mug shots so every later panel renders the same person. Use when the author types /kav-character <name>, adds or changes a character, or when a character keeps coming out looking wrong."
argument-hint: "<name> [--story <slug>]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-character.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-character`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-character.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
