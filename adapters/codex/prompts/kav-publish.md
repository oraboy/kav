---
description: "Build the readers for every drawn chapter of a story, link them chapter to chapter and back home, and explain the publishing surfaces (static HTML on any host, Netlify, GitHub Pages; Instagram carousel JPEGs; PDF later). Use when the author types /kav-publish, says \"publish the story\", \"put it online\", \"build all the readers\", or asks where they can post the book."
argument-hint: "[--story <slug>]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-publish.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-publish`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-publish.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
