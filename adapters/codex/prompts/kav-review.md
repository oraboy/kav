---
description: "Open the local image review page for a panel batch, wait for the author to pick, reroll and edit text, then read the saved review JSON and apply it (picks recorded, rerolls regenerated with a changed line, text replaced verbatim). Use when the author types /kav-review <batch.json>, says \"open the review page\", \"show me the takes\", or \"picks are in\"."
argument-hint: "<batch.json>"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-review.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-review`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-review.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
