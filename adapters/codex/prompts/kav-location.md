---
description: "Bring a place into a story — take the author's photos (or a description, or a generated candidate they pick) and register it so every later panel draws the same place. Use when the author types /kav-location <name>, drops photos of a place, says \"add a location\", \"new place\", or when a place keeps coming out wrong."
argument-hint: "<name> [--story <slug>]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-location.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-location`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-location.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
