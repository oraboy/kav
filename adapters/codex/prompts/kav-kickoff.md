---
description: "Run the story kickoff — the non-linear, file-persisted working meeting that scaffolds stories/<slug>/ and fills it block by block (concept, cast, locations, objects, style, visual lock, pitch with shape and intention/obstacle, storyboard, package and brief). Use when the author types /kav-kickoff <slug>, says \"new story\", \"kickoff\", \"scaffold a story\", or wants to resume a kickoff in progress. Interactive, with hard stops at every creative decision; never autopilot."
argument-hint: "<slug>"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-kickoff.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-kickoff`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-kickoff.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
