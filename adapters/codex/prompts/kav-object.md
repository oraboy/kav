---
description: "Bring an object into a story — a thing that must look exactly the same every time (a can, a car, a dress, a sword, a branded box) — from the author's photo, and register it so every panel draws that exact thing. Use when the author types /kav-object <name>, says \"add an object\", \"add a prop\", drops a photo of a thing, or when an object keeps coming out different."
argument-hint: "<name> [--story <slug>]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-object.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-object`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-object.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
