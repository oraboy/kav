---
description: "Jot down anything about the story — a scene, a situation, a visual, an object, a line, a what-if — without arguing it, in the story's pitch-inbox.md, where it shows on the board's Ideas tab and Kav picks it up when it fits. Also removes or rewords notes (/kav-note remove N003). Use when the author types /kav-note, says \"note this\", \"jot this down\", \"remember this for later\", or pins an idea on the board."
argument-hint: "<note> | remove N00N"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-note.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-note`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-note.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
