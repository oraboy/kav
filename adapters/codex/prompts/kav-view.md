---
description: "Pull up the story board — rebuild it from the story's files, file any ideas pinned on it, and put it in front of the author beside the chat. Use when the author types /kav-view, says \"show me the board\", \"open the map\", \"where are we\", \"refresh the board\", or \"check the board\". /kav-view ideas opens it on the Ideas tab."
argument-hint: "[ideas]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-view.md. Edit the canonical file, then re-run. -->

You are running the Kav command `kav-view`.

1. Make sure the current working directory is the Kav repo (it contains `AGENTS.md` and `kav/commands/`). If it isn't, ask the author where Kav is cloned and work from there.
2. Read `kav/commands/kav-view.md` in full and follow it exactly.
3. Also respect the hard rules in `AGENTS.md`.

Arguments from the author: $ARGUMENTS
