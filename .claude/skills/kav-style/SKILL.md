---
name: kav-style
description: "Register a set of reference images as a named visual style pack (styles/<pack>/ plus a medium.txt line), and link it into a story. Use when the author types /kav-style <pack>, drops style reference images, or asks to add, define or test a look (e.g. \"add these as a style called noir\")."
argument-hint: "<pack> [--story <slug>]"
---

<!-- Generated by scripts/sync_adapters.py from kav/commands/kav-style.md. Edit the canonical file, then re-run. -->

# /kav-style <pack> — define a look by example

A **style pack** is a folder of reference images that define a visual language, plus one line of prose (`medium.txt`). Passed to the image model beside the cast and location references, it steers the rendering by example — no adjective guessing.

**Story scope:** packs are the one thing that may be shared across stories — they live in `styles/<pack>/` at the repo root — but a story uses only the pack its `style/style.md` names, linked at `stories/<slug>/styles/<pack>` (a symlink to the shared pack, or a real folder if the pack is the story's own). A pack existing in `styles/` is not permission to use it for a new story; ask.

## Registering a pack

1. **Find the images.** Ask the author where they are (a folder, their Downloads). If it isn't obvious which files are meant, list recent images and confirm — don't guess.
2. **Pick the name** — short, lowercase, no spaces (`noir`, `watercolor-kids`).
3. **Copy (don't move) into `styles/<pack>/`.** `.jpg`/`.png`/`.webp`; sent in sorted filename order. Three images is the working default (2–5 is fine).
4. **Look at every image.** Note in one line what the pack actually carries — line quality, palette, shading, abstraction. If the images don't share one coherent language, say so: mixed looks average into mush.
   - **Keep faces out of the pack** — a recognisable face competes with the cast for identity.
   - **Watch for text** — titles and logos in a reference bleed into output as lettering.
5. **Write `styles/<pack>/medium.txt` — style as code.** This one line is injected into every prompt ("Render the entire final image as …"). Its wording has outsized impact; an ambiguity becomes per-seed randomness.
   - **Rendering only, never content.** Any noun that could be an object in the world gets painted into every frame — including frames where it can't exist (a line mentioning "clouds and foliage" puts sky into an underground room). Name light, colour relationships, line quality, shading, texture. Never sky, weather, landscape, foliage, time of day or props. Close with: *"render only what the scene describes — this style governs how the image is drawn, never what is in it."*
   - **Describe, don't label.** Replace words with two readings ("monochromatic", "painterly", "vintage") with what you mean ("one bold colour cast over the whole frame").
   - **Name palette poles as relationships**, not as coloured things: "a warm-to-cool contrast between lit and shadowed areas", not "orange sky and teal trees".
   - **Close the wrong readings** ("always richly coloured, never grayscale").
   - **Test on a portrait too** — a portrait has no location reference, so the medium line is the only background instruction; content smuggled into the line shows up there first.
6. **Add a short entry to `styles/README.md`**: the pack name, what it looks like, the medium line.
7. **Link it into the story** (when a story is active): `stories/<slug>/styles/<pack>` → symlink to `../../../styles/<pack>` (or copy if the author wants the story self-contained); copy the references into `stories/<slug>/style/moodboard/`; write or update `style/style.md` from `docs/templates.md`, including the **lettering theme** (caption and balloon colours and fonts that suit the look).
8. **Bind it for generation:** set `defaults.style_pack: "<pack>"` in `stories/<slug>/briefs.json` as soon as the story adopts the pack. The symlink and `style.md` are documentation; this field is what puts the pack into a prompt when a scene line doesn't name it. Check with a `--dry-run` line that omits the pack name and confirm the style resolves.

## Testing it

Quick look: a few cheap generations on the explore lane, e.g.

```
python3 tools/generate.py --story <slug> "<a character from the story> in <a location of the story>, <pack>" --lane seedream --ar 4:5
```

**Put a cast face in at least one test.** A pack that looks right on rooms can still render people badly, and that is what the book is made of. Conversely, keep faces that resemble the cast *out of the pack itself* — the pack teaches rendering, and a look-alike in it competes with the character's own references. A pack with no close-up face at all teaches nothing about faces; one with unrelated faces is what you want.

Vary the scene: one interior, one bright daylight exterior, one portrait. **Styles drift toward ordinary colour illustration on bright outdoor scenes** — the location photo's own light fights the pack — so test a daylight exterior before adopting. The full, gated test is `/kav-visual-style-lock <pack>`.

## Cautions worth raising once

- A pack lifted from a recognisable property (a film, a show, a living illustrator's body of work) produces work that reads as that property. Style steering is normal practice, but a book that reads as someone else's show is a weaker signature. Say it plainly once, then respect the decision.
- Inconsistency across samples almost always means `medium.txt` is ambiguous. Fix the language before blaming the model or the seed.
